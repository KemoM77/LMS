const firebase = {
  initializeApp: () => {},
};

const getFirestore = () => ({});
const doc = () => ({});
const getDoc = () => ({});

// Export the mocked auth module
export { getFirestore, doc, getDoc };
export default firebase;
